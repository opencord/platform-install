default: manual
environments:
    manual:
        type: manual
        bootstrap-host: juju.cord.lab
        bootstrap-user: ubuntu
        default-series: {{ ansible_distribution_release }}
        bootstrap-timeout: 900 # 15 minutes
